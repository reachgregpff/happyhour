<section class="info">
	<div class="detail">
		<div class="bar-list">
		<p class="location"><%= @location %></p>
		<!-- <p>hello <%#= @address %></p>
		<p><%#= @postcode %></p>
		<p><%#= @day %></p>
			<%# @bars.each do |bar| %>
				<p><%#= bar.name %></p>
				<p><%#= bar.longitude %></p>
				<p><%#= bar.latitude %></p> -->
			<%# end %>
		</div>
	</div>

	<div class="map">
		
	</div>
</section>

 <script id="bar-box-template" type="text/x-underscore-template">
    
      <img src={{ image_url }}>
      <header class="content">
        <h1>{{ name }}</h1>
      </header>
      
  </script>

<script>
 $('document').ready(initMap());
</script>